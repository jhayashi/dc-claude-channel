# Override compose env vars by copying this to `.env`. Defaults in
# docker-compose.yml are sufficient for normal use.

# MAIL_DOMAIN=_chatmail.test
# Override only if 8443/10465/10993 conflict with another local service.
# RELAY_HTTPS_PORT=8443
# RELAY_SMTPS_PORT=10465
# RELAY_IMAPS_PORT=10993
